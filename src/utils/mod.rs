//! Small, generic helpers that don't belong to any one feature — as
//! opposed to `moderation/`, `discord/`, etc., which are all organized
//! around a specific concern. Keep this module thin: something only
//! belongs here once at least two unrelated features need it.

pub mod ids;
